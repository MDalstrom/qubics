[package]
name = "qubics"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
qubics-macros = { path = "../qubics-macros" }
linkme = "0.3.35"

[build-dependencies]
qubics-build = { path = "../qubics-build" }
