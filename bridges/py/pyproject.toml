[project.scripts]
qubics = "_epk:main"

[tool.ruff.lint.per-file-ignores]
"src/_epk/setup.py" = ["F401", "F403"]

[project]
name = "qubics-bridge"
version = "0.0.1"
dependencies = [
  "numpy",
  "toml"
]

[project.optional-dependencies]
dev = [
  "epk",
  "pycparser"
]
